# PetVerse V13.1 战斗生产切片验证报告

## 本阶段范围

本阶段把战斗从“图片碰撞”推进到可替换正式资源的 3D 生产切片，覆盖：

- PET001 炎尾狐、PET002 岩甲龟、PET008 森灵鹿与 BOSS001 古树守卫；
- 进场、待机、普攻、主动技能、受击、死亡、胜利七套动作合同；
- 普通五宠战、首领战、集火、阵法能量与阵法大招；
- 首领预警、下一回合专属技能与二阶段转场；
- 服务端判定、稳定事件 ID、掉线恢复数据与幂等结算；
- 正式 Prefab 未就绪时的程序化 3D 样板回退。

本阶段没有把参考图打进运行包，也没有把未经过模型、动作、包体和真机验收的资源标记为正式资源。

## 实际接口验证

验证账号：开发测试账号 `userId=203`。为覆盖三只代表宠物，测试阵容使用 PET001、PET002、PET008、PET004、PET006；本次仅对开发数据库增加了一只 PET008 测试宠物。

### 普通战

- 会话：`1720`
- battleId：`2e399d22-9db9-4ed9-9fd8-59c16a09c4b5`
- 结果：我方胜利，第 6 回合结束
- battleLog：149 条，eventId 全部唯一
- 已验证：PET001/PET002/PET008 均进入出战快照、集火目标切换、阵法能量增长、阵法大招、胜负结束

### 首领战视觉机制

- 会话：`1729`
- battleId：`46f956db-989a-4142-8a56-5ebac6550477`
- 结果：我方胜利，第 9 回合结束
- battleLog：226 条，eventId 全部唯一
- 首领预警：1 次
- 首领二阶段：1 次
- 我方阵法大招：2 次
- 敌方阵法大招：2 次
- 集火目标自动迁移：5 次

该中等难度用例用于验证首领血量阶段和玩家阵法大招；战斗结束前预警出现，但首领在下一回合前被击败，因此没有强行补发技能，符合“预警可被集火打断”的战斗结果。

### 首领专属技能

- 会话：`1723`
- battleId：`80d37199-cb1d-44e2-b363-1ca03cffe2d3`
- 难度：1.5
- 结果：敌方胜利，第 13 回合结束
- battleLog：298 条，eventId 全部唯一
- 首领预警：3 次
- 首领专属技能“古树根震”：3 次

原逻辑把首领机制能量与普通主动技能能量共用，普通技能会先消耗能量，导致首领专属技能无法稳定释放。现已改为独立 `bossEnergy`，并采用“达到阈值预警，下一回合释放”的可读机制。高难度真实接口用例确认连续三次预警均在下一回合释放。

## 结算幂等

普通战会话 `1720`：

- 首次结算成功，金币 192、五宠经验 144；
- 同一结算键再次请求返回 `duplicate=true`；
- 奖励状态保持 `claimed`，未重复发奖。

首领战会话 `1729`：

- 首次结算成功，金币 384、五宠经验 264；
- 同一结算键再次请求返回 `duplicate=true`；
- 奖励状态保持 `claimed`，未重复发奖。

## 资源状态

四个角色均已建立：

- 生产规格；
- 四视图参考；
- Cocos 路径合同；
- 动作命名合同；
- 程序化 3D 样板；
- 正式 Prefab 加载与安全回退。

`formalAssetReady` 仍保持关闭。这表示当前样板可以验证镜头、体型、站位、动作节奏和技能识别，但不能冒充已经由 DCC 工具完成蒙皮与动作的正式模型。

## 自动检查

- 战斗垂直切片审计：42/42
- 宠物生产合同审计：63/63
- 客户端 TypeScript：通过
- 服务端热编译：0 Error
- UI 架构与输入层级：纳入最终回归

## 当前唯一外部依赖

正式骨骼模型仍需要 Blender/Maya 等 DCC 工具或合格的外包资产生产。仓库当前具备无损替换正式 Prefab 的接口，不需要为了换模型重写战斗逻辑。

Cocos Creator 当前机器出现开发者登录窗口；这不影响代码、接口和自动审计，但最终编辑器预览、微信构建与真机录像仍需要有效的 Cocos 登录会话。
